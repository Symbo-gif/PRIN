Kernel Architecture
===================

.. note::
   Written during Phase 3. Documents the single-source CubeCL kernel design
   that replaces PRINet 3.0's triple implementation (Triton / CUDA JIT /
   PyTorch fallback), backend dispatch, and the kernel-equivalence test
   methodology.
