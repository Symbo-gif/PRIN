sphinx>=7.0
furo>=2024.1.29
myst-parser>=3.0

# Transitive dependency minimums to satisfy Snyk Open Source (WP002-F2).
# These packages are pulled in by sphinx/furo and must be at least the
# patched versions listed below; the constraints are enforced by pip's resolver.
idna>=3.15
jinja2>=3.1.6
pygments>=2.20.0
requests>=2.33.0
soupsieve>=2.8.4
urllib3>=2.7.0
